//
//  IdeasForSkills.swift
//  Skills 3
//
//  Created by Jayce Sagvold on 11/14/22.
//

import SwiftUI

struct IdeasForSkills: View {
    var body: some View {
        VStack {
            Text("Building new skill ideas")
                .padding(.top, 16.7)
                .bold()
                .font(.system(size: 31))
                .fontWeight(.black)
                .padding(.bottom, 16.7)
        
            VStack {
                Image("PersonCoding")
                    .resizable()
                    .frame(height: 175)
                    .cornerRadius(30)
                Text("Computer Programming")
                    .font(.title3)
                    .padding(.bottom, 3.5)
                Text("Pick a programming language and find a tutorial")
                    .padding(.bottom, 6.5)
            }
            
            VStack {
                Image("PersonDrawing")
                    .resizable()
                    .frame(height: 175)
                    .cornerRadius(30)
                Text("Drawing")
                    .font(.title3)
                    .padding(.bottom, 3.5)
                Text("Learning how to be a good drawer can make you $")
                    .padding(.bottom, 6.5)
            }

            VStack {
                Image("PersonTyping")
                    .resizable()
                    .frame(height: 135)
                    .cornerRadius(30)
                Text("Typing speed")
                    .font(.title3)
                    .padding(.bottom, 3.5)
                Text("Get work done faster by having a fast typing speed. ")
                
                VStack {
                    Image("Reading")
                        .resizable()
                        .frame(height: 175)
                }
            }
        }

        
    }
}

struct IdeasForSkills_Previews: PreviewProvider {
    static var previews: some View {
        IdeasForSkills()
    }
}
